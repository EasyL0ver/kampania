# Template Fix Report

Records content **cut or moved** when files in `characters/`, `events/`, and
`locations/` were reformatted to full conformance with their templates and
`actions-and-opportunities.md`.

Each file below lists, per source file, what was removed and why, so it can be
reviewed and re-homed (e.g. atmosphere → GM notes, history → `historical context/`,
plot → `story-facts/`) by hand later.

Batches:
- `characters-*.md`
- `events-*.md`
- `locations-*.md`
